export enum WorkflowState {
  Locking = 'locking',
  Locked = 'locked',
}

export enum GroupCallingLocationStatus {
  New = 0,
  Edit = 1,
}

export enum CustomizationTabsList {
  Schedule = 'schedule',
  Calls = 'calls',
  Chats = 'chats',
  Contacts = 'contacts',
  Files = 'files',
}

export enum CustomizationMainMenus {
  Starred = 'starred',
  Roles = 'roles',
  Schedules = 'schedules',
  TextNonUsers = 'text_non_users',
  VisitPath = 'visit_path',
}

export enum CustomizationSecondaryMenus {
  Archived = 'archived',
  EditProfile = 'edit_profile',
  Feedback = 'feedback',
  Settings = 'settings',
  Logout = 'logout',
}

export enum CustomizationOtherMenus {
  CareChannel = 'care_channel',
  OnCallGroups = 'on_call_groups',
  Barcode = 'barcode',
  Snap = 'snap',
}

export enum Platform {
  Windows = 'Windows',
}
